# ~/.config/starship.toml

format = """
$directory\
$git_branch\
$git_status\
$c\
$elixir\
$elm\
$golang\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$rust\
$docker_context
$time\
[ ](fg:#21bfc2)\

"""

[git_state]
format = '[\($state( $progress_current of $progress_total)\)]($style) '
cherry_pick = "[🍒 PICKING](bold red)"

[nodejs]
format = "via [🤖 $version](bold green) "

[docker_context]
format = "via [🐋 $context](blue bold)"

# ~/.config/starship.toml

[cmd_duration]
min_time = 500
format = "time [$duration](bold yellow)"

[time]
#時間の表示をオンにして12時間表示に
disabled = false
style = "bg:#21bfc2 black bold"
format = '[$time]($style)'
time_format = '%m/%d(%A) %T'
